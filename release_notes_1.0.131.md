Release 1.0.131
----------------

- Training: added explicit pause markers (<pause>, [pause], {pause}, pause_marker) to control TTS pauses and avoid mid-sentence stops.
- Training flag: continues to respect local overrides/stored flags without forcing training when set to N.
- Tyre checklist: enforced driver selection before vehicle and passes driver/inspector IDs to inspections.
- Sorting: training modules sorted numerically by code (training_2 before training_10).









