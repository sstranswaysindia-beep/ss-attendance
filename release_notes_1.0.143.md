Release 1.0.143
----------------

- Training: realtime training_req redirect (web + mobile) and clears flag on completion.
- Training: fixed progress display by passing currentUser to SafetyRepository.
- Training UI: added Logout button in header.
- Training Player: keeps screen awake during playback and resumes from same position.
