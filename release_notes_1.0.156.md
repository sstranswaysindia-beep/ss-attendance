# Release Notes – 1.0.156

- Disable Android font scaling across the app using `MediaQuery.textScaler = TextScaler.noScaling` (prevents Android “Font size” setting from changing in-app text).
- Dashboard UI spacing tweaks (AppBar green icon spacing; welcome/name alignment).
- Khata Book: entry details + edit flow improvements (category rules, driver/vehicle display, receipt view).

Build:
- Target: `lib/main.dart`
- Output: `build/app/outputs/bundle/release/app-release.aab`
- Dart defines:
  - `ANDROID_REWARDED_AD_UNIT_ID=ca-app-pub-6163248993890252/1567348677`
  - `IOS_REWARDED_AD_UNIT_ID=` (empty)

