Release 1.0.142
----------------

- Training: fixed pause markers to pause only after a chunk completes (prevents pausing before last word).
- Training: enabled transcript TTS in browser preview (Web Speech API).
- Training UI: moved time estimate before the play/pause icon.
- Safety: drivers can open Safety but only Training is visible.
