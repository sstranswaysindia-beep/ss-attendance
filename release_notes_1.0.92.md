# Release 1.0.92 (Build 92)

- Adds supervisor "Mark Absent" toggle API plumbing so admins can enforce attendance directly from the app.
- Aligns trip overview endpoint with legacy behaviour to restore driver/helper listings on mobile.
- Rebuilt driver/supervisor Android bundle (`app-release.aab`) with the updated 1.0.92+92 metadata.
