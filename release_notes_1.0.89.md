# Release 1.0.89 (Build 89)

- Added enable/disable toggles in Driver Master so admins can flip `drivers.status` without leaving the list.
- Geofencing and Proxy Attendance controls now share the refreshed gradient styling and green status switches for consistency.
- Flutter/Android metadata bumped to 1.0.89+89; admin bundle expected at `build/app/outputs/bundle/release/admin-app-release.aab` after build.
