# Release 1.0.114 (build 114)

- Fund transfers now carry sender/receiver labels with plant names for clearer audit trails.
- Advance entries keep the description selector visible, hide extra notes for advances, and auto-label with the selected driver (including plant).
- Driver search chips show plant names to reduce ambiguity.

Build command:
`flutter build appbundle --release --dart-define=ANDROID_REWARDED_AD_UNIT_ID=ca-app-pub-6163248993890252/1567348677 --dart-define=IOS_REWARDED_AD_UNIT_ID= --build-name=1.0.114 --build-number=114 --target lib/main.dart`
