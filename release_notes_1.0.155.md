# Release Notes – 1.0.155

- Khata Book: tap any entry to view full details with receipt link and edit/delete controls; edit flow now preserves category rules and shows vehicle/driver info and IDs.
- Category rules: enforced driver-required categories (incentive, shoes, salary, training, travel, travel and uniform, uniform, driver name); hide safety/advance office; category flags for driver/vehicle pulled from backend.
- UI: larger fallback profile animation; notification bell animation; dashboard header tweaks; animations for headers and profile placeholders.
- Text scaling locked to 1.0 to avoid Android system font/display size affecting the app.
- Built app bundle with rewarded ad unit dart defines.

