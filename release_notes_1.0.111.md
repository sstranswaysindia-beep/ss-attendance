# SS Transways India Admin 1.0.111

- Supervisor approvals filter fixes for both the Flutter admin build and the PHP web panel (person-type toggle, API role param).
- Web admin page now lets admins switch between supervisors/drivers and reuses the mobile API data.
- Build: admin-only Flutter target (`lib/main_admin.dart`) bundled with rewarded-ad dart defines, packaged as `admin-app-release.aab` under the `com.sstranswaysindia.admin` id.
